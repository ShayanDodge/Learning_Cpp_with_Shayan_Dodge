// if statements

#include<iostream>

int main()
{
    int b = 10;

    std::cout<<"This program checks the value of b.\n";

    if(b == 6)
    {
        std::cout<<"b is equal to a half dozen.\n";
    }

    std::cout<<"There is not much to say about a\n";
    return 0;
}
